# Session Summary — Sistema de Irrigação Seletiva Linear v1.0

---

## Session 2026-03-08 (parte 4 — Hikvision, agua_consumida, RETOMAR, systemd)

### Session Date: 2026-03-08

### Project State at Session End

**Status:** Sistema refatorado com nova câmera RTSP Hikvision, acumulador de água consumida, suporte a retomada de sessão (RETOMAR), e serviço systemd instalado na Raspberry Pi. Aguardando atualização do projeto HMI no PIStudio (novo label 40002, divisor 40003, botão RETOMAR) e teste de campo com câmera e trator reais.

---

### Hardware Change — Câmera Hikvision DS-2CD1021G0-I

A câmera Wanscam JW0004 (MJPEG over HTTP, WiFi) foi substituída pela Hikvision DS-2CD1021G0-I (RTSP, Ethernet).

- IP câmera: `192.168.1.64`
- Conexão: eth0 dedicado na Pi, subnet `/24`
- Pi eth0: `192.168.1.1/24` (gateway para a câmera)
- Pi wlan0: `172.16.148.73/24` (hotspot Shimura-cel — acesso de campo)
- Acesso à interface web em campo: `http://172.16.148.73:5000`

---

### Changes in detector_unificado.py

1. **distancia default:** 2.0m → 2.5m (distância real entre mudas no campo)
2. **MIN_PERIOD:** 0.3s → 1.5s (= 6 km/h com 2.5m — evita disparos falsos em alta velocidade)
3. **tankvolume substituído por agua_consumida:**
   - Antes: volume restante do tanque (decrementado, começa em 100L)
   - Agora: acumulador de água consumida (começa em 0, incrementa a cada acionamento)
   - Razão: mais intuitivo para operador de campo ("quantos litros gastamos hoje")
4. **Novos args CLI:** `--agua_consumida_inicial`, `--seedlingcount_inicial`, `--pll_period_inicial` (para suporte ao modo RETOMAR)
5. **PLLController:** aceita `agua_consumida_inicial` e `pll_period_inicial` no construtor
6. **seedlingcount:** inicializa de `args.seedlingcount_inicial` (não sempre de 0)
7. **manual_valve() público no PLLController:** antes era lógica inline; agora método explícito chamado por valve_trigger e pelo Modbus comando 3

---

### Changes in modbus_server.py

1. **R_TANK (40002):** lê `agua_consumida` diretamente em litros (sem ×10 — raw inteiro)
2. **R_SPEED (40003):** `tractorspeed × 100` — HMI deve usar divisor /100 (antes era ×10)
3. **R_DISTANCIA default:** 20 → 25 (= 2.5m)
4. **Cooldown 3s:** agora aplicado apenas a comandos 1 (INICIAR) e 2 (PARAR) — removido para comando 3 (IRRIGAR) para permitir acionamentos múltiplos pelo operador
5. **Comando 4 = RETOMAR:** novo comando no registrador 40011. Chama callback `on_retomar` registrado pelo app.py. Reinicia o detector preservando seedlingcount, agua_consumida e pll_period.

---

### Changes in app.py

1. **_increment_agua_consumida():** função que incrementa `agua_consumida` em `data.json` quando o detector está OFF (para contabilizar irrigações manuais)
2. **manual_valve e _do_valve:** incrementam `agua_consumida` no data.json quando detector está OFF
3. **start_detector(resume=False):**
   - `resume=False` (padrão): zera `agua_consumida` e `seedlingcount` em data.json antes de iniciar
   - `resume=True`: preserva `seedlingcount`, `agua_consumida` e `pll_period` do data.json e passa como args iniciais ao subprocesso
4. **Rota /retomar:** nova rota POST. Chama `start_detector(resume=True)`.
5. **modbus.on_retomar:** callback registrado que chama a rota /retomar
6. **Args iniciais passados ao subprocesso:** `--agua_consumida_inicial`, `--seedlingcount_inicial`, `--pll_period_inicial` lidos de data.json quando resume=True

---

### Changes in templates/index.html

- Label "Tanque" → "Água Consumida"
- ID `tankvolume` → `agua_consumida` no elemento de exibição e no polling JavaScript

---

### New File: irrigacao.service

Serviço systemd instalado na Raspberry Pi em `/etc/systemd/system/irrigacao.service`.

```
WorkingDirectory=/home/sergio/yolo
ExecStart=/home/sergio/yolo/py313env/bin/python app.py
Restart=on-failure
RestartSec=5
```

Habilitado com `systemctl enable irrigacao` — auto-start no boot. Logs via `journalctl -u irrigacao`.

---

### HMI — Atualizações Pendentes no PIStudio

O projeto HMI no PIStudio precisa ser atualizado para refletir as mudanças desta sessão:

| Componente | Antes | Agora |
|---|---|---|
| Numeric Display 40002 | Label "Tanque", divisor /10 | Label "Agua Cons.", sem divisor (raw) |
| Numeric Display 40003 | divisor /10 | divisor /100 |
| Botão novo | — | RETOMAR: escreve 4 em 40011 |

Após atualizar: gerar UDisk WMT3 e fazer upload via Backstage do HMI.

---

### Key Decisions

1. **agua_consumida como acumulador** é mais claro para operador que volume restante — evita confusão com "tanque cheio antes de começar"
2. **MIN_PERIOD 1.5s** (não 0.3s) previne que ruído de detecção ou frames duplicados criem período absurdo
3. **resume=True preserva estado** — permite parar e retomar sem perder contagem de mudas ou água gasta
4. **Cooldown 3s removido para IRRIGAR** — operador pode precisar irrigar manualmente várias vezes seguidas
5. **systemd para auto-restart** — essencial para uso em campo sem PC de suporte; também resolve reinicializações acidentais da Pi

---

### Next Steps (Priority Order)

1. Atualizar projeto HMI no PIStudio: label 40002, divisor 40003, botão RETOMAR (valor 4 em 40011)
2. Gerar UDisk WMT3 e fazer upload via Backstage do PI3070ig
3. `git pull` na Raspberry Pi + `sudo systemctl restart irrigacao`
4. Testar fluxo completo: INICIAR via HMI → detecções → agua_consumida incrementando → PARAR → RETOMAR (estado preservado)
5. Calibrar MIN_PERIOD e pll_period_inicial com velocidade real do trator
6. Avaliar uso de CPU — considerar modo int8 se necessário

---

## Session 2026-02-22

### Session Date: 2026-02-22

### Project State at Session End

**Status:** Servidor Modbus RTU implementado e integrado ao Flask. Mapa de registradores definido para HMI WECON PI3070ig. Aguardando configuração do PIStudio e deploy no Raspberry Pi.

---

### Problem Resolved

**Erro `NameError: name 'get_data' is not defined`** no Raspberry Pi (IP 192.168.15.10) rodando código antigo em `/home/sergio/yolo/app.py`. A versão atual em `/home/sergio/Linear/app.py` já possui a correção — função renomeada para `get_data_from_file()` em sessão anterior. O Raspberry Pi ainda não recebeu o código atualizado. Solução: `git pull` ou `rsync` no Pi.

---

### New Functionality Implemented

#### `modbus_server.py` (criado do zero)

Servidor Modbus RTU serial para comunicação com HMI WECON PI3070ig via conversor USB-RS232 (`/dev/ttyUSB0`).

**Arquitetura interna:**
- `_CallbackBlock(ModbusSequentialDataBlock)` — subclasse com hook `setValues()` que dispara callback quando o HMI escreve um registrador
- `ModbusRTUServer` — classe principal com:
  - `_handle_write()` — processa comandos do HMI (start/stop/válvula manual)
  - `_update_loop()` — thread daemon que lê `data.json` a cada 0.5s e atualiza os registradores de monitoramento
  - `_run_loop()` / `_serve()` — asyncio em thread daemon separada para o servidor serial
  - API pública: `start()`, `stop()` + callbacks injetáveis: `on_start`, `on_stop`, `on_valve`, `get_status`

**Dependência instalada:** `pymodbus 3.12.1` no venv.

#### `app.py` (modificado)

Adicionados ao arquivo existente:
- `from modbus_server import ModbusRTUServer` (import no topo)
- Instância global: `modbus = ModbusRTUServer(port='/dev/ttyUSB0', baudrate=9600)`
- No bloco `__main__`: injeção de callbacks + `modbus.start()`

---

### Technical Decisions Made

1. **Protocolo: Modbus RTU via RS232 serial** (não Ethernet)
   - Razão: a porta Ethernet do Pi já está ocupada pela câmera IP (192.168.1.64). O conversor USB-RS232 já estava disponível no hardware.
   - Configuração: slave ID=1, `/dev/ttyUSB0`, 9600 bps, 8-N-1

2. **HMI: WECON PI3070ig** — software de programação: PIStudio V9.5.9
   - Resolução: 800×480, touch screen
   - Download do software: `https://ftp.we-con.com.cn/Download/Software/PIStudio_9.5.9.25091702.zip`

3. **pymodbus 3.12.1 — API moderna**
   - Usa `ModbusDeviceContext` (não o antigo `ModbusSlaveContext`)
   - Usa `ModbusServerContext(devices=store, single=True)`
   - Aplica offset +1 internamente: índice no bloco = endereço PDU + 1, logo registrador HMI = 40000 + índice

4. **Mapa de registradores 1-based (índices no bloco)**
   - Registradores 1-7: leitura pelo HMI (dados do sistema)
   - Registradores 11-18: escrita pelo HMI (parâmetros e comandos)
   - Registrador 11 (HMI 40011): comando com auto-reset para 0 após processamento

5. **Isolamento por callbacks:** `ModbusRTUServer` não importa nem depende de `app.py` diretamente. Toda integração é feita por injeção de callbacks (`on_start`, `on_stop`, `on_valve`, `get_status`). Isso permite testar o servidor Modbus isoladamente.

6. **Thread daemon para o servidor Modbus:** não bloqueia o Flask. O servidor asyncio roda em event loop próprio em thread separada. O update loop roda em outra thread separada.

---

### Register Map (Holding Registers)

| Índice no bloco | PDU Modbus | HMI PIStudio | Descricao | Escala |
|---|---|---|---|---|
| 1 | 0 | 40001 | seedlingcount | x1 |
| 2 | 1 | 40002 | tankvolume | x10 (956 = 95.6 L) |
| 3 | 2 | 40003 | tractorspeed | x10 (52 = 5.2 km/h) |
| 4 | 3 | 40004 | pll_sync_lost | 0/1 |
| 5 | 4 | 40005 | pll_missed_count | x1 |
| 6 | 5 | 40006 | pll_period | x100 (201 = 2.01 s) |
| 7 | 6 | 40007 | detector_running | 0/1 |
| 11 | 10 | 40011 | comando (1=start, 2=stop, 3=valvula) | auto-reset |
| 12 | 11 | 40012 | modo (0=fast,1=balanced,2=onnx,3=int8) | x1 |
| 13 | 12 | 40013 | limiar | x100 (50 = 0.50) |
| 14 | 13 | 40014 | delay | ms |
| 15 | 14 | 40015 | distancia | x10 (20 = 2.0 m) |
| 16 | 15 | 40016 | volume_tanque | L |
| 17 | 16 | 40017 | volume_irrigacao | x10 (50 = 5.0 L) |
| 18 | 17 | 40018 | max_corr | % |

---

### Ideas Explored but Not Implemented This Session

- **Modbus via Ethernet (TCP):** Descartado — porta Ethernet ocupada pela câmera.
- **Coils para comandos booleanos:** Optou-se por Holding Registers para comandos também (registrador 40011 com valor 1/2/3), para manter tudo em uma única tabela e simplificar a configuração no PIStudio.
- **Histórico de irrigações no HMI:** Fora do escopo desta sessão. O HMI exibirá apenas estado atual.

---

### Tensions and Open Issues

1. **Deploy no Raspberry Pi ainda não foi feito.** O código atual (com Modbus) está somente no PC de desenvolvimento. O Pi em 192.168.15.10 ainda roda o código antigo de `/home/sergio/yolo/`.
2. **pymodbus 3.12.1 não está instalado no Pi.** Precisará de `pip install pymodbus` no venv do Pi.
3. **PIStudio ainda não foi instalado nem configurado.** A tela HMI ainda não existe.
4. **Teste de comunicação Pi ↔ HMI** ainda não foi realizado.
5. **Proteção contra volume negativo do tanque** ainda não implementada (item herdado da sessão anterior).

---

## Session 2026-02-18

### Session Date: 2026-02-18

### Project State at Session End

**Status:** Interface web funcional com PLLController implementado. Sistema pronto para testes em campo no Raspberry Pi 5.

---

### Structural Decisions Made

1. **Flask + subprocesso:** `app.py` gerencia `detector_unificado.py` como subprocesso separado, comunicação via `data/data.json`. Isso permite reiniciar o detector sem derrubar o servidor web e facilita o deploy na Raspberry Pi.

2. **PLLController como classe dedicada:** O PLL foi encapsulado em uma classe dentro de `detector_unificado.py`, com métodos `on_detection()` e `_pll_fire()`. Isso torna o código mais testável e mantém clara a separação entre lógica de detecção e lógica de sincronismo.

3. **Debounce interno ao PLL:** O debounce (40% do período) foi implementado dentro do `PLLController.on_detection()`, não no loop principal. Decisão: manter a lógica de rejeição de detecções duplicadas próxima ao estado do PLL.

4. **Slew rate limiter (`max_corr`):** Limita a variação máxima do período por detecção. Default 20% (corrigido de 10% após análise — 10% era muito restritivo para variações de velocidade do trator). Parâmetro passado via CLI.

5. **Parada na perda de sincronismo:** Após 3 disparos consecutivos sem detecção real, o PLL para de acionar a válvula e sinaliza `pll_sync_lost = true`. Re-sincroniza automaticamente na próxima detecção real.

6. **Interface web 3 colunas:** Layout redesenhado para mostrar: [contador de mudas | volume do tanque | velocidade do trator] em linha, câmera centralizada abaixo, botões de ação abaixo da câmera, alerta de perda de sincronismo PLL em destaque.

### Content Choices Finalized

- **Parâmetros expostos na UI:** modo, limiar, delay, distancia, volume_tanque, volume_irrigacao, max_corr. Todos passados como args para o subprocesso.
- **ROI configurável:** clique e arraste na imagem da câmera para definir ROI; salva em `data/roi_config.json`; persiste entre sessões.
- **Acionamento manual da válvula:** botão na interface para testes sem detecção.
- **Cálculo de velocidade:** `3.6 × distancia / period` (km/h), exibido na UI em tempo real.

### Problems Identified and Solved

- **Problema:** Múltiplas detecções do mesmo objeto em frames consecutivos geravam irrigações duplicadas.
  **Solução:** Debounce de 40% do período estimado dentro do PLLController.

- **Problema:** Variações abruptas de velocidade do trator causavam período PLL instável.
  **Solução:** Filtro IIR (α=0.3) + slew rate limiter (max_corr) para suavizar atualizações de período.

- **Problema:** GPIO no Raspberry Pi 5 usa chip 4, diferente dos modelos anteriores.
  **Solução:** Código tenta chip 4 primeiro, fallback para chip 0.

- **Problema:** `static/live.jpg` sendo servido como arquivo estático causava caching no browser.
  **Solução:** Endpoint `/video_feed` com MJPEG stream e timestamp na URL da imagem para forçar refresh.

### Ideas Explored but Rejected

- **WebSocket para dados em tempo real:** Rejeitado por complexidade de deploy na Raspberry Pi. Polling HTTP a cada 1s é suficiente para os requisitos.
- **SQLite para histórico de irrigações:** Rejeitado por enquanto — `data.json` simples atende a necessidade imediata. Pode ser adicionado futuramente.
- **Modo multiclass (eucalipto + madeira):** O modelo suporta 2 classes, mas o sistema de irrigação usa apenas detecções de eucalipto por ora. Madeira pode ser filtrada como obstáculo no futuro.

### Evolution of the Concept

- **Início:** Script simples de detecção com saída no terminal.
- **Meio:** Adição de GPIO e contador de mudas.
- **Fim da sessão 2026-02-18:** Sistema completo com interface web, PLL sincronizado, ROI configurável e múltiplos modos de inferência.
- **Fim da sessão 2026-02-22:** Camada de comunicação HMI adicionada via Modbus RTU — o sistema agora pode ser operado sem acesso a PC/browser.

---

## Session 2026-02-23

### Session Date: 2026-02-23

### Project State at Session End

**Status:** Tela HMI criada e configurada no PIStudio V9.5.9. Aguardando geração do arquivo UDisk (WMT3, aba HMI V2.0) e upload via pendrive FAT32 para o hardware PI3070ig.

---

### Problem Resolved

**PIStudio nao abria o dialogo de Download via cabo USB:** O botao "Download" no PIStudio nao abre nenhuma janela para o PI3070ig. Solucao confirmada: usar "UDisk Download" — exportar para pendrive FAT32 e inserir no HMI. Formato obrigatorio: WMT3, aba "HMI V2.0" (nao V1.0).

**Bit Lamp nao funcionava com holding registers:** O componente Bit Lamp do PIStudio monitora apenas coils (bobinas). Para holding registers (40xxx), o componente correto e o Word Lamp, com condicoes de valor (ex: valor 0 = estado OFF, valor 1 = estado ON/ALERTA).

**Driver USB CP210x necessario no Windows:** O cabo USB PC-HMI requer o driver CP210x para ser reconhecido. Instalacao feita no PC Windows.

---

### New Functionality Implemented

#### Tela HMI no PIStudio V9.5.9

Projeto criado em `F:\Agenda\FSP\P&D\Linear - Bolson\HMIProject\HMIProject.pi` (Windows).

**Hardware configurado:** WECON PI3070ig, resolucao 800x480, touch screen.

**Comunicacao Modbus configurada:**
- Porta: COM1 (USB-RS232)
- Protocolo: RS232, Modbus RTU Slave (All Function)
- Velocidade: 9600 bps, 8-N-1
- Device No.: 1

**Secao Monitoramento (leitura de registradores):**

| Componente | Registrador HMI | Escala | Descricao |
|---|---|---|---|
| Numeric Display | 40001 | x1 | Contagem de mudas |
| Numeric Display | 40002 | /10 | Volume do tanque (L) |
| Numeric Display | 40003 | /10 | Velocidade do trator (km/h) |
| Numeric Display | 40006 | /100 | Periodo PLL (s) |
| Numeric Display | 40005 | x1 | Falhas PLL consecutivas |
| Word Lamp | 40004 | 0=SYNC OK / 1=SYNC LOST | Indicador de sincronismo PLL |
| Word Lamp | 40007 | 0=OFF / 1=ON | Indicador de detector ativo |

**Secao Configuracao (escrita de registradores):**

| Componente | Registrador HMI | Escala | Descricao |
|---|---|---|---|
| Drop Down List | 40012 | 0=fast/1=balanced/2=onnx/3=int8 | Modo de inferencia |
| Numeric Input | 40013 | /100 | Limiar de confianca |
| Numeric Input | 40014 | ms | Delay valvula |
| Numeric Input | 40015 | /10 | Distancia entre mudas (m) |
| Numeric Input | 40016 | L | Volume inicial do tanque |
| Numeric Input | 40017 | /10 | Volume por irrigacao (L) |
| Numeric Input | 40018 | % | Correcao maxima do periodo |

**Botoes de acao (escrita em 40011 com auto-reset):**

| Botao | Valor escrito | Acao |
|---|---|---|
| INICIAR | 1 | start_detector() |
| PARAR | 2 | stop_detector() |
| PURGAR | 3 | manual_valve() — irrigacao de limpeza |

---

### Technical Decisions Made

1. **Word Lamp obrigatorio para holding registers**
   - Razao: Bit Lamp no PIStudio so monitora coils (funcao Modbus 01). Para holding registers (funcao 03/06), usar Word Lamp com condicoes de valor.
   - Impacto: sem alteracao no firmware — os registradores 40004 e 40007 ja eram inteiros 0/1.

2. **Download obrigatorio via UDisk (pendrive FAT32)**
   - Razao: Download via cabo USB nao funciona para o modelo PI3070ig no PIStudio V9.5.9 (botao nao abre dialogo).
   - Procedimento: PIStudio > UDisk Download > selecionar pasta > copiar para pendrive FAT32 > inserir no HMI.
   - Formato correto: WMT3, aba "HMI V2.0" (a aba V1.0 gera formato incompativel).

3. **Botao PURGAR = valvula manual temporaria**
   - Escreve valor 3 no registrador 40011.
   - O servidor Modbus no Pi chama o callback `on_valve`, que aciona o GPIO por 200ms.
   - Equivalente ao botao "Manual Valve" na interface Flask.
   - Nome "PURGAR" escolhido para contexto de campo: operador usa para testar/limpar a valvula antes da operacao.

4. **PIStudio V9.5.9 confirmado compativel com PI3070ig (serie ig)**
   - A versao V9 suporta a serie ig; versoes mais antigas podem nao suportar.

---

### Ideas Explored but Not Implemented This Session

- **Duas telas separadas (Monitoramento e Configuracao):** Optou-se por uma unica tela para simplificar a operacao em campo — o operador nao precisa navegar entre telas.
- **Alarmes sonoros no HMI:** Nao implementado por ora; o Word Lamp SYNC LOST ja fornece alerta visual suficiente para esta fase.
- **Historico de irrigacoes no HMI:** Fora do escopo. O HMI exibe apenas o estado atual.

---

### Tensions and Open Issues

1. **UDisk ainda nao gerado nem instalado no HMI.** Proximo passo imediato.
2. **Deploy no Raspberry Pi ainda nao realizado.** Codigo com Modbus esta somente no PC de desenvolvimento. O Pi em 192.168.15.10 ainda roda codigo antigo.
3. **pymodbus 3.12.1 nao instalado no Pi.** Requer `pip install pymodbus` no venv.
4. **Teste de comunicacao Pi <-> HMI** ainda nao realizado.
5. **Protecao contra volume negativo do tanque** ainda nao implementada (item herdado).

---

## Next Steps (Priority Order — apos sessao 2026-02-23)

1. **[PROXIMO]** Gerar arquivo UDisk no PIStudio: menu UDisk Download > formato WMT3, aba HMI V2.0
2. Copiar pasta gerada para pendrive FAT32
3. Inserir pendrive no PI3070ig e aguardar upload automatico
4. Deploy do codigo no Raspberry Pi: `git pull` + `pip install pymodbus` no venv
5. Conectar cabo USB-RS232 entre Pi (/dev/ttyUSB0) e HMI (COM1)
6. Testar comunicacao: verificar leitura de registradores no HMI e envio de comandos
7. Calibrar parametros PLL com dados de campo reais
8. Adicionar protecao contra volume negativo do tanque no detector_unificado.py

---

## Key Files Modified Per Session

### Sessao 2026-02-23

| Arquivo | Mudanca |
|---|---|
| `CLAUDE.md` | Nova entrada de sessao 2026-02-23 adicionada |
| `GEMINI.md` | Sincronizado com CLAUDE.md |
| `v1.0 - session-summary.md` | Sessao 2026-02-23 documentada |
| `README.md` | Status atualizado |
| `F:\...\HMIProject.pi` | Projeto HMI criado no PIStudio (Windows, fora do repo) |

### Sessao 2026-02-22

| Arquivo | Mudanca |
|---|---|
| `modbus_server.py` | Criado do zero — servidor Modbus RTU completo |
| `app.py` | Import + instancia + start do ModbusRTUServer adicionados |

### Sessao 2026-02-18

| Arquivo | Mudanca |
|---|---|
| `app.py` | Criado do zero — servidor Flask completo |
| `detector_unificado.py` | PLLController adicionado; parametros distancia, volume_tanque, volume_irrigacao, max_corr |
| `templates/index.html` | Redesign completo: 3 colunas, alerta PLL |
| `static/style.css` | Estilos da nova interface |
| `CLAUDE.md` | Documentacao completa do projeto |
| `.claude/agents/script-session-closer.md` | Agente de fechamento de sessao |
| `.gitignore` | Exclusoes para venv, modelos, arquivos runtime |
